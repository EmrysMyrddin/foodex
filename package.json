{
  "private": true,
  "scripts": {
    "dev": "docker-compose up -d && yarn hasura console",
    "hasura": "hasura --project hasura --skip-update-check",
    "deploy:hasura": "yarn hasura migrate apply --endpoint https://foodex-db.caprover.cocaud.dev --admin-secret $HASURA_ADMIN_SECRET"
  },
  "devDependencies": {
    "hasura-cli": "1.2.0-beta.1"
  }
}
